<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Bitcoin Price – <%= currency %></title>
  <link rel="stylesheet" href="/style.css" />
</head>

<body class="result-page">
  <div class="container">
    <% if (price) { %>
      <div class="price-container card">
        <h2 class="row" style="margin:0;">
          <span class="pill">BTC → <%= currency %></span>
          <span>
            Current price:
            <span class="price"><%= price %></span>
          </span>
        </h2>
      </div>
    <% } else { %>
      <div class="card">
        <h2 class="error">Sorry, currency not found or error occurred.</h2>
      </div>
    <% } %>

    <div class="row" style="margin-top: 8px;">
      <a href="/" class="back-link">⬅ Try another currency</a>
      <div class="select-wrap">
        <label for="timeRange">Range</label>
        <select id="timeRange">
          <option value="7">1 Week</option>
          <option value="30">1 Month</option>
        </select>
      </div>
    </div>

    <div class="card chart-card">
      <h2 class="chart-title" style="margin-top:0;">Historical Price Chart</h2>
      <!-- Chart container wrapper ensures fixed height -->
      <div class="chart-wrapper">
        <canvas id="priceChart"></canvas>
      </div>
      <div class="muted" id="chartNote"></div>
    </div>
  </div>

  <!-- Pass currency from server to JS -->
  <script>
    const selectedCurrency = "<%= currency.toLowerCase() %>";
  </script>

  <!-- Chart.js (use exact stable version) -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<!-- Your custom script (must come AFTER Chart.js) -->
<script src="/script.js"></script>

</body>
</html>
